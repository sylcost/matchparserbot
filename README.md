# matchparserbot

Install PYTHON 2.7

`pip install opencv-python Flask-PyMongo jsonmerge pytube pymongo apscheduler flask`


Install NodeJS

`npm install`

`npm run build`
 
 to be continued

`node server.js`

`python server.py`
